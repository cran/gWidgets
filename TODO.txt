gframe, gexpandgroup should get a horizontal= argument
FIX FONTS!! weight <-> style


DONE * pmg -- close buttons in linux -- read e-mail
DONE? * ggroup == on.dragmotion -- not working under windows, linux OK
  (my guess is that is liew in getWindow()$Raise()

***** gdf -- error with editing see 20 vs 22

* Handle drag out events in ggrphics (select a rectangle.). Return a
  rectangle: c(x0,x1, y0, y1) as in 

* export .getWidget, .getBlock, .getParent (Michaels request)

* ghtml widget (for RwxWidgets)
* Debian package on CRAN for R
